<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.isuyo_000.activities">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FrontLayout"
            android:label="@string/title_front_layout"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserData.UserList"
            android:label="@string/title_userList"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ChannelLimits"></activity>
        <activity
            android:name=".Graph.PlotGraph"
            android:label="@string/title_userList"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".Graph.GraphList"
            android:label="@string/title_userList"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>